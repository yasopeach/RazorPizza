@page
@model RazorPizza.Pages.Admin.PizzaOrderListingModel
@{
}

<div class="container">
    <h1 class="text-danger">ADMIN PANEL</h1>
    <div class="">


    <table class="table table-dark table-hover text-danger">
        <thead>
            <tr>
                <td>Pizza Name</td>
                <td>Price</td>
                <td>Delete</td>
            </tr>
        </thead>


        <tbody>
            @foreach (var po in Model.PizzaOrders)
            {

                <tr>
                    <td>@po.PizzaName</td>
                    <td>@po.Price</td>
                    <td>
                        <div>
@*                            <a asp-page="Edit" asp-route-id="@po.PizzaName" class="btn btn-danger mx-2">
                                <i class="bi bi-pencil-square"></i>
                            </a>*@
                            <a asp-page="Delete" asp-route-id="@po.Price" class="btn btn-danger mx-2">
                                <i class="bi bi-x-square"></i>
                            </a>
                        </div>  
                    </td>
                </tr>
            }

            <tr>
                <td></td>
                <td></td>
                <td></td>
            </tr>
        </tbody>
    </table>

    </div>
</div>